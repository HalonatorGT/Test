const listitems = document.querySelectorAll('list-item');

listitems.forEach(function(element, index) {
  element.innerHTML = 'Hello World ' + (index + 1);
});




for (let i = 0; i < 5; i++) {
  console.log('Hello ' + i);
}


// for each list instructions
